let username = 'vijay';
console.log('Name = ${username.toUpperCase()}');
console.log(`Name = ${username.toUpperCase()}`);
let random = Math.random();
if(random >= 0.5) {
    console.log(`Random number is >= 0.5 i.e., ${random}`);
} else {
    console.log(`Random number is < 0.5 i.e., ${random}`);
}